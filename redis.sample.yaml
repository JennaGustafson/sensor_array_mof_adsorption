# Please copy this file to redis.yaml and change the connection string to
# point to your redis instance, or just use the below default local redis connection
# string for testing. (You still need a local redis server installed and runnign for this to work!)

redis:
  host: 'localhost'
  port: 6379
  db: 0
queue: 'sensor_ads'
